<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    >
    <data class="DeviceItemBinding">
     <variable
         name="device"
         type="com.rye.catcher.beans.binding.DeviceBean"/>
    </data>
<LinearLayout
    android:layout_width="match_parent"
    android:layout_height="@dimen/len_80"
    android:orientation="horizontal"
    android:background="@color/soft1"
    >
 <TextView
     android:id="@+id/title"
     android:layout_width="0dp"
     android:layout_height="match_parent"
     android:layout_weight="1"
     android:gravity="center"
     android:textStyle="bold"
     android:textSize="@dimen/font_18"
     android:text="@{device.title}"
     tools:text="@string/cancel"
     />
    <TextView
        android:id="@+id/content"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:gravity="center"
        android:textSize="@dimen/font_14"
        android:text="@{device.content}"
        tools:text="@string/cancel"
        />

    <View
        android:layout_width="@dimen/len_2"
        android:layout_height="match_parent"
        android:background="@color/black"
        android:layout_marginRight="@dimen/len_8"
        />
</LinearLayout>
</layout>